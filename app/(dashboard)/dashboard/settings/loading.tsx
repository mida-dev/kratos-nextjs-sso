import { DashboardLoading } from "@/components/dashboard/dashboard-loading";

/**
 * Renders the loading state for the settings view.
 */
export default function SettingsLoading() {
  return <DashboardLoading variant="settings" />;
}
